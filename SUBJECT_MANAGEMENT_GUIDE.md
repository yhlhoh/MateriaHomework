# 科目管理功能使用指南

## 功能概述

新的科目管理系统使用**单独对话框**集中管理所有科目，支持显示/隐藏、拖动排序、添加和删除功能。

## 如何使用

### 1. 打开科目管理对话框

- 点击右下角的**菜单按钮**（齿轮图标或清除图标）
- 在弹出菜单中选择 **"管理科目"**
- 科目管理对话框将打开

### 2. 显示/隐藏科目

在科目管理对话框中：

- 找到需要隐藏的科目
- 点击右侧的 **开关** 关闭
- 关闭的科目将在主看板上隐藏
- 再次打开开关可恢复显示

**注意**：隐藏的科目数据仍然保存，不会丢失。

### 3. 重新排序科目

#### 使用拖动功能：

1. 在科目管理对话框中，找到需要移动的科目
2. 将鼠标悬停在科目左侧的 **拖动句柄**（≡ 符号）上
3. 按住并拖动科目到新的位置
4. 拖入目标位置时会出现顶部蓝线提示
5. 放开鼠标完成排序

**注意**：排序会自动保存。

### 4. 添加新科目

在科目管理对话框中：

1. 点击底部的 **"添加新科目"** 按钮
2. 在打开的对话框中：
   - 输入 **科目名称**（如 "信息技术"）
   - 从下拉菜单选择 **科目图标**
3. 点击 **"确定"** 添加
4. 新科目将出现在看板上

### 5. 删除科目

在科目管理对话框中：

1. 找到需要删除的科目
2. 点击右侧的 **关闭按钮**（✕）
3. 在确认对话框中点击 **"确定"**
4. 科目将被永久删除（包括其内容）

⚠️ **警告**：删除操作不可撤销，请谨慎操作。

## UI 组件说明

### 科目项布局

```
[≡] [图标] 科目名称        [开关] [×删除]
```

- **≡**：拖动句柄
- **[图标]**：科目图标
- **[开关]**：显示/隐藏开关
- **[×]**：删除按钮

### 颜色设计

- 科目项背景：浅粉色（`surface-variant`）
- 文字颜色：棕色（`on-surface`）
- 删除按钮：红色（`error`）
- 拖动提示：主色（粉红色）

## 数据持久化

所有改动自动保存：
- 科目顺序
- 显示/隐藏状态
- 新增/删除的科目
- 科目内容

刷新页面后数据仍然保留。

## 快速操作

| 操作 | 方式 |
|------|------|
| 隐藏科目 | 在管理对话框中关闭开关 |
| 显示科目 | 在管理对话框中打开开关 |
| 重新排序 | 拖动科目到新位置 |
| 添加科目 | 点击"添加新科目"按钮 |
| 删除科目 | 点击科目右侧的关闭按钮 |

## 常见问题

**Q：删除科目后能恢复吗？**
A：不能。删除是永久的，包括科目的所有内容。如果只是想暂时隐藏，请使用开关功能。

**Q：能否修改科目名称？**
A：当前版本不支持修改名称，只能删除并重新添加。

**Q：拖动排序支持多个科目同时拖动吗？**
A：不支持，只能一个一个拖动。

**Q：数据存储在哪里？**
A：数据存储在浏览器的 LocalStorage 中，不会上传到服务器。
